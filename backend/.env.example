# App
PORT=3001
NODE_ENV=development

# Supabase
SUPABASE_URL=https://SEU-PROJETO.supabase.co
# Use apenas no backend: chave de service role do Supabase
SUPABASE_SERVICE_ROLE_KEY=coloque_aqui
# Opcional: anon key, se necessÃ¡rio no servidor
SUPABASE_ANON_KEY=opcional

# JWT (caso valide tokens emitidos pelo Supabase)
SUPABASE_JWT_SECRET=opcional

# ConexÃ£o direta com Postgres (se usar ORM/migraÃ§Ãµes)
DATABASE_URL=postgresql://usuario:senha@host:porta/banco?sslmode=require

# Prisma (se estiver usando)
PRISMA_DATABASE_URL=
SHADOW_DATABASE_URL=
